# Макс - Мини-приложение для студентов

Веб-приложение для управления расписанием занятий, базой знаний, опросами и другими функциями для студентов.

## 🚀 Технологии

### Backend
- Python 3.10+
- Django 5.2
- Django REST Framework
- Channels (WebSocket)
- SQLite (разработка) / PostgreSQL (продакшн)

### Frontend
- React 19.2
- TypeScript
- Material-UI
- FullCalendar
- React Router

## 📋 Требования

Перед установкой убедитесь, что у вас установлено:

- Python 3.10 или выше
- Node.js 16+ и npm
- Git

## 🛠 Локальная установка и запуск

### 1. Клонирование репозитория

```bash
git clone https://github.com/saxtamazer/max.git
cd max
```

### 2. Настройка Backend

#### 2.1. Создание виртуального окружения

```bash
cd backend
python3 -m venv venv
```

#### 2.2. Активация виртуального окружения

**Linux/Mac:**
```bash
source venv/bin/activate
```

**Windows:**
```bash
venv\Scripts\activate
```

#### 2.3. Установка зависимостей

```bash
pip install -r requirements.txt
```

#### 2.4. Настройка базы данных

Выполните миграции для создания структуры базы данных:

```bash
python manage.py migrate
```

#### 2.5. Создание суперпользователя

Создайте администратора для доступа к панели управления:

```bash
python manage.py createsuperuser
```

Следуйте инструкциям на экране для ввода имени пользователя, email и пароля.

#### 2.6. Сбор статических файлов (опционально)

```bash
python manage.py collectstatic --noinput
```

#### 2.7. Запуск сервера разработки

```bash
python manage.py runserver
```

Backend будет доступен по адресу: http://127.0.0.1:8000/

Админ-панель: http://127.0.0.1:8000/admin/

### 3. Настройка Frontend

Откройте новый терминал (не закрывая backend сервер).

#### 3.1. Переход в директорию frontend

```bash
cd frontend
```

#### 3.2. Установка зависимостей

```bash
npm install
```

#### 3.3. Запуск сервера разработки

```bash
npm start
```

Frontend будет автоматически открыт в браузере по адресу: http://localhost:3000/

## 📁 Структура проекта

```
max-mini-app/
├── backend/
│   ├── api/                  # API приложение
│   ├── backend_core/         # Основные настройки Django
│   ├── knowledge_base/       # База знаний (статьи)
│   ├── polls/               # Опросы
│   ├── schedule/            # Расписание занятий
│   ├── media/               # Загружаемые файлы
│   ├── staticfiles/         # Собранные статические файлы
│   ├── manage.py
│   └── requirements.txt
│
└── frontend/
    ├── public/              # Публичные файлы
    ├── src/
    │   ├── components/      # React компоненты
    │   ├── layouts/         # Макеты страниц
    │   ├── pages/          # Страницы приложения
    │   ├── App.tsx
    │   └── index.tsx
    ├── package.json
    └── tsconfig.json
```

## 🔧 Настройка переменных окружения

### Backend

Для продакшн окружения создайте файл `.env` в директории `backend/`:

```env
SECRET_KEY=ваш-секретный-ключ
DEBUG=False
ALLOWED_HOSTS=yourdomain.com,www.yourdomain.com
DATABASE_URL=postgresql://user:password@localhost/dbname
```

### Frontend

Создайте файл `.env` в директории `frontend/`:

```env
REACT_APP_API_URL=http://127.0.0.1:8000
```

## 📱 Основные функции

- **📅 Расписание занятий** - просмотр расписания с фильтрацией по неделям и группам
- **📚 База знаний** - статьи и учебные материалы с категориями
- **📊 Опросы** - создание и прохождение опросов
- **📆 Экспорт в календарь** - добавление занятий в Google Calendar, Apple Calendar и т.д.
- **👤 Админ-панель** - управление контентом через Django Admin

## 🔐 Доступ к админ-панели

После создания суперпользователя:

1. Перейдите на http://127.0.0.1:8000/admin/
2. Войдите используя созданные учетные данные
3. Управляйте контентом: статьи, опросы, расписание и т.д.

## 🐛 Решение проблем

### Backend не запускается

1. Убедитесь, что виртуальное окружение активировано
2. Проверьте, что все зависимости установлены: `pip install -r requirements.txt`
3. Выполните миграции: `python manage.py migrate`

### Frontend не запускается

1. Удалите `node_modules` и `package-lock.json`
2. Выполните `npm install` заново
3. Проверьте версию Node.js: `node -v` (должна быть 16+)

### Ошибки CORS

Убедитесь, что в `backend/backend_core/settings.py` настроен CORS:

```python
CORS_ALLOWED_ORIGINS = [
    "http://localhost:3000",
    "http://127.0.0.1:3000",
]
```

## 📝 Разработка

### Создание миграций после изменения моделей

```bash
cd backend
python manage.py makemigrations
python manage.py migrate
```

### Сборка продакшн версии Frontend

```bash
cd frontend
npm run build
```

Собранные файлы будут в директории `frontend/build/`

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для новой функции (`git checkout -b feature/AmazingFeature`)
3. Закоммитьте изменения (`git commit -m 'Add some AmazingFeature'`)
4. Запушьте в ветку (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## 📄 Лицензия

Этот проект создан для образовательных целей.

## 👨‍💻 Автор

**saxtamazer**

GitHub: [@saxtamazer](https://github.com/saxtamazer)

## 📞 Поддержка

Если у вас возникли проблемы или вопросы, создайте [Issue](https://github.com/saxtamazer/max/issues) на GitHub.
